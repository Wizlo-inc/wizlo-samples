PORT=3065

# Wizlo backend the widget will talk to, and the M2M credentials used to mint a
# USER-scoped token via /oauth/user-token. Keep the secret server-side only.
WIZLO_BASE_URL=https://api-uat.wizlo.com
WIZLO_CLIENT_ID=your_client_id
WIZLO_CLIENT_SECRET=your_client_secret
